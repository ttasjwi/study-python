# 식별자 - 캐멀케이스 (대문자로 시작 -> 클래스)
class HelloMachine:
    def foo(self):
        print('hello')

# 식별자 - 스네이크 케이스 (소문자로 시작)
machine = HelloMachine()
machine.foo()

# 함수 : 뒤에 괄호가 있다
print("nyaa")

# 변수 : 뒤에 괄호가 없다
number_of_people = 5
