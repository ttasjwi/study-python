# min(리스트) : 리스트 내부 최솟값
# max(리스트) : 리스트 내부 최댓값
# sum(리스트) : 리스트 내부 값을 모두 합산

numbers = [103, 52, 273, 32, 77]
print(min(numbers))
print(max(numbers))
print(sum(numbers))

# min(인자1, 인자2, ...) : 인자들 중 최솟값
# max(인자1, 인자2, ...) : 인자들 중 최댓값
print(min(1, 2))
print(max(1, 2))
